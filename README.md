# Proyecto API - Gestión de Planes Académicos

## Requisitos
- Docker y Docker Compose

## Instrucciones

```bash
cd laravel-app
cp .env .env.local
docker-compose up -d --build
```

Luego visita:

- Documentación Swagger: http://localhost:8000/api/documentation
